# Security Model

## 1. 보호 대상

- 보험계약, 특약, 보험료·가입금액, 증권 식별자
- 진단·처치·검사·입퇴원 등 의료 민감정보
- 가족관계와 Guardian 권한
- 신분·지급·계좌 관련 문서
- 약관 탐지 결과, 청구 서류, 보험사 결과
- 전문가 의견과 분쟁 자료

## 2. 위협 모델

주요 위협은 계정 탈취, 가족 데이터의 과도한 열람, 내부자 오남용, 업로드 문서 유출, 파트너 오발송, 로그·분석도구 유출, 규칙/약관 변조, 잘못된 결과의 대량 배포, 삭제 미전파다.

## 3. 최소수집과 목적 제한

- 후보 탐지에 필요한 계약/이벤트/문서만 단계적으로 요청한다.
- 주민번호 전체, 계좌 원문, 건강정보는 화면·로그·분석 이벤트에서 기본 마스킹한다.
- 마케팅, 모델 학습, 제품 분석은 각각 별도 동의와 거부 경로를 둔다.
- 원문 문서 보관 없이 추출값만으로 가능한 단계는 원문을 즉시 폐기할 수 있게 한다.

## 4. 암호화

- 전송: TLS 1.3 우선, 파트너 mTLS/요청 서명 검토
- 저장: 데이터 계층 AES-256, 사용자/tenant 단위 envelope key
- 키: KMS/HSM, 역할 분리, 순환, 폐기 감사
- 모바일: OS Keychain/Keystore에 짧은 session/unwrap key만 저장
- 업로드: client-side checksum, 짧은 signed URL, malware scan, MIME 재검증

Phase 0의 AsyncStorage에는 테스트 fixture와 진행 상태만 저장한다. 실제 의료·보험 원문을 저장하지 않는다.
파일 선택기의 실제 파일명도 저장하지 않으며, 검수하지 않은 파일은 청구 준비 완료로 계산하지 않는다.

## 5. 인증과 접근제어

- passkey/강한 MFA를 우선하고 고위험 작업에 step-up 인증
- 서비스 간 workload identity와 최소권한
- 사용자/가족/전문가/지원/파트너 역할을 분리한 RBAC + resource attribute 검사
- Guardian은 사람별·데이터 종류별·기간별 위임, 언제든 철회 가능
- 지원 인력은 승인·사유·시간제한이 있는 break-glass만 허용

## 6. 감사

다음 이벤트는 append-only 감사 대상이다.

- 동의 생성·변경·철회
- 계약/약관/진료/문서 접근과 export
- 후보 생성·변경·설명 노출
- 전문가 이관·열람·의견 작성
- 청구 묶음 생성·제출·취소
- 결과 수신과 규칙 검토
- 권한·키·보존정책 변경
- 삭제 요청·전파·완료

로그에는 원문 진단명, 문서 내용, 전체 증권번호를 기록하지 않는다. tamper-evident chain과 별도 보존정책을 적용한다.

## 7. 안전한 규칙 공급망

- rule bundle은 코드리뷰, 30개+ 회귀 테스트, compliance 승인을 통과한다.
- bundle manifest에 버전, hash, 유효기간, 작성자, 승인자를 서명한다.
- 앱/서버는 서명 검증 실패 시 새 규칙을 사용하지 않는다.
- canary 배포와 즉시 rollback을 제공한다.
- 모델 prompt/output은 규칙 번들로 취급하지 않으며 판정 권한을 갖지 않는다.

## 8. 모바일 보안

- 화면 전환/백그라운드 시 민감 화면 가림
- root/jailbreak, 디버깅, 변조 신호는 위험 기반 step-up에 활용하되 단독 차단은 신중히 적용
- clipboard 복사와 screenshot은 민감도에 따라 경고/차단
- deep link는 허용 목록과 1회성 token 사용
- 업로드 파일은 앱 캐시에 남지 않도록 수명 관리

## 9. 사고 대응

1. 탐지와 correlation ID 확보
2. 영향 tenant/user와 데이터 종류 식별
3. token/key/partner route 격리
4. 증거 보존과 법적 통지 시계 시작
5. 사용자에게 사실·영향·조치·지원 채널 안내
6. 근본 원인, 회귀 테스트, 재발 방지 완료 후 복구

## 10. 보안 출시 게이트

- 데이터 흐름/위협 모델 리뷰
- 모바일·API·클라우드 침투 테스트
- 권한 matrix와 Guardian abuse-case 테스트
- 삭제·export·동의 철회 end-to-end 테스트
- dependency/SBOM/secret scan
- 파트너 보안평가와 사고 통지 SLA
- 백업 복원 및 crypto-shredding 검증

## 11. 외부 서비스와 API 키

- 공식 웹 연결에는 사용자 계약·진료·청구 데이터를 URL parameter로 붙이지 않는다.
- 가까운 병원 검색은 사용자 동의 후 지도 중심 좌표만 지도·장소 제공사에 보내며 자체 서버에는 위치 이력을 저장하지 않는다. 지도 화면과 개인정보 안내에 제3자 전송 사실을 표시한다.
- 공개 지도 검색은 짧은 세션 캐시를 사용해 같은 위치의 반복 전송을 줄이고, 병원 응답에 없는 사진·전화·진료시간을 추정하거나 생성하지 않는다.
- 외부 기관의 로그인과 본인인증은 해당 기관 origin에서만 진행한다.
- `EXPO_PUBLIC_*` 환경값은 모바일/web bundle에서 읽을 수 있으므로 비밀로 취급할 수 없다.
- Phase 0의 HIRA 공공데이터 개발키는 로컬 시험만 허용한다. 운영 호출은 서버 proxy에서 키를 보관하고 domain allowlist, rate limit, cache, timeout, response schema 검증을 적용한다.
- 기관별 adapter는 출처, 호출시각, 응답코드, 데이터 버전을 남기되 개인 민감정보와 키를 로그에 기록하지 않는다.
