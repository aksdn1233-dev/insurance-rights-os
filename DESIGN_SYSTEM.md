# Design System

## 1. 경험 원칙

사용자가 기술을 의식하지 않고 “보험이 이렇게 쉬웠나?”라고 느끼는 것이 목표다. 화면은 돈을 과장하지 않고, 해야 할 다음 행동과 그 이유를 차분하게 보여준다.

1. 한 화면에는 하나의 주 결정을 둔다.
2. 결과보다 근거의 이해 순서를 우선한다.
3. 전문용어는 보조 라벨로 내리고 일상어를 제목으로 올린다.
4. 빈 공간을 정보 구조로 사용한다.
5. 애니메이션은 상태 변화 확인에만 짧게 사용한다.
6. 초등학교 고학년이 한 번 읽고 이해할 수 있는 문장을 쓴다.
7. 금융앱의 빠른 리듬과 명확한 다음 행동은 따르되 특정 제품의 화면을 복제하지 않는다.

## 2. 금지 시각 언어

- AI, 로봇, 반짝이, 마법, 보라색 SaaS 관용구
- 과도한 gradient, glassmorphism, card-in-card
- 의미 없는 숫자 대시보드
- 금화, 현금, 지폐 이미지
- 보험 상담원 stock photo
- 무조건 지급을 암시하는 축하 폭죽이나 수익 연출
- 토스 또는 애플의 고유 패턴을 그대로 복제하는 구성

## 3. 색상

| Token | 값 | 용도 |
|---|---:|---|
| `canvas` | `#F7F8FA` | 가벼운 회색 기본 배경 |
| `surface` | `#FFFFFF` | 주요 읽기 면 |
| `ink` | `#191F28` | 기본 텍스트 |
| `muted` | `#6B7684` | 보조 설명 |
| `line` | `#E5E8EB` | 구분선 |
| `brand` | `#3182F6` | 주 행동, 선택 상태 |
| `brandSoft` | `#E8F3FF` | 낮은 강조 |
| `info` | `#4E5968` | 근거와 정보 |
| `warning` | `#B35C00` | 추가 확인 필요 |
| `danger` | `#E64B3C` | 오류·거절, 과장 금지 |

색상만으로 상태를 전달하지 않고 아이콘/라벨/문장을 함께 제공한다.

## 4. Typography

- 시스템 산세리프를 기본으로 사용해 OS와 언어 렌더링 품질을 따른다.
- Display: 32/39, 700 — 홈의 한 문장
- Title 1: 26/33, 700 — 화면 제목
- Title 2: 20/27, 700 — 주요 섹션
- Body: 16/24, 400 — 기본 설명
- Body strong: 16/24, 650 — 행동/핵심
- Caption: 13/19, 500 — 출처, 버전, 상태
- 숫자는 금액 자랑이 아니라 날짜·서류 수·상태에만 제한적으로 사용한다.

## 5. 공간과 형태

- 4pt base, 기본 화면 좌우 여백 20pt
- 섹션 사이 32–40pt, 관련 행 사이 12–16pt
- 주요 surface radius 24pt, 입력 18pt, pill 999pt
- 그림자 대신 배경 대비와 1px line을 우선한다.
- 중첩 surface는 최대 한 단계. 카드 안에 카드 금지.

## 6. 핵심 컴포넌트

- `Page`: Safe area, 최대 읽기 폭, 스크롤 동작
- `SectionHeader`: 짧은 제목 + 필요한 경우 한 줄 설명
- `PrimaryAction`: 동사 중심, 한 화면 1개
- `QuietAction`: 보조/취소/나중에
- `StatusPill`: 확인할 항목, 더 알아보기, 준비됨, 제출됨
- `EvidenceRow`: 찾은 이유, 가입할 때의 약관, 원문 보기
- `DocumentRow`: 필요한 이유, 준비됨/필요함 상태
- `Timeline`: 입력 → 발견 → 준비 → 제출 → 결과
- `Disclosure`: “왜 이런 결과가 나왔나요?”의 접기/펼치기
- `TestDataBanner`: 연습용 화면이며 실제 청구되지 않음을 지속적으로 알림

## 7. 홈 구성

상단에는 “병원에 다녀오셨나요?”라는 익숙한 질문을 둔다. 첫 행동은 “보험 확인 시작하기”이며, 확인할 보험이 있으면 한 건씩 보여준다. “내가 할 일”에는 계약·서류·결과에서 지금 처리할 것만 모은다. 최근 치료 떠올리기는 이미 받은 진료를 기억하도록 돕되 새 진료를 유도하지 않는다.

## 7.1 첫 설치 길잡이

- 홈/가족/병원/내 보험을 4개의 짧은 화면으로 안내한다.
- 한 화면에는 숫자 1개, 탭 이름, 설명 1개, 기억할 문장 1개만 둔다.
- `다음`과 마지막 `시작하기`만 주 버튼으로 사용한다.
- 완료 후 자동으로 다시 띄우지 않으며 `내 보험`에서 다시 볼 수 있다.
- 마지막 화면에서 환급금 찾기의 위치와 역할을 함께 알린다.

## 8. 모션과 촉각

- 화면 진입 180–240ms fade/translate
- readiness 변경 160ms, 진행 막대만 애니메이션
- 주 단계 완료 시 가벼운 selection haptic 1회
- 거절 결과에는 진동·강한 빨강 애니메이션을 사용하지 않는다.
- Reduce Motion 설정을 존중한다.

## 9. 접근성

- 본문 대비 WCAG AA 이상
- 터치 영역 최소 44×44pt
- Dynamic Type에서 200%까지 핵심 행동과 근거가 잘리지 않게 한다.
- 스크린리더 라벨은 상태+대상+다음 행동 순으로 작성한다.
- 시간 제한 과업 없음. 업로드/입력은 언제든 중단·재개 가능.

## 10. 카피 기준

기본 말투는 짧고 존중하는 해요체다. 한 문장에는 한 가지 뜻만 담고, 24자 전후에서 자연스럽게 끊는다. “피보험자”는 “보험 대상”, “보장 개시”는 “보험 시작일”, “약관 버전”은 “가입할 때의 약관”, “Claim Readiness”는 “준비한 서류”로 바꾼다. “수령 가능”, “고액”, “혜택 당첨” 대신 “확인할 보험”, “찾은 이유”, “필요한 서류”를 쓴다. 위험 고지는 숨기지 않되 모든 화면에 반복해 불안을 만들지 않는다. 중요한 결정 직전에 맥락형 고지를 배치한다.
